<style>
  body, html {
    margin: 0; 
    padding: 0; 
    height: 100vh; 
    font-family: Arial, sans-serif;
    overflow: hidden;
  }
  .container {
    display: flex;
    height: 100vh;
    background: linear-gradient(135deg, #ffffff, #ffeded, #d40000);
  }
  .left {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    color: #fff;
    background: linear-gradient(135deg, #d40000, #a10000);
  }
  .login-box {
    background: #fff;
    padding: 40px 30px;
    border-radius: 8px;
    width: 320px;
    box-shadow: 0 4px 25px rgba(0,0,0,0.2);
    color: #333;
  }
  .logo {
    display: flex;
    justify-content: center;
    margin-bottom: 25px;
  }
  .logo img {
    height: 50px;
  }
  .right {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #fff;
  }
  .right img {
    height: 60vh;
    max-width: 80%;
    object-fit: contain;
    border-radius: 15px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.1);
  }
  .login-box input[type="email"],
  .login-box input[type="password"] {
    width: 100%;
    border: none;
    border-bottom: 1px solid #aaa;
    margin-bottom: 25px;
    padding: 8px 0;
    font-size: 16px;
    outline: none;
    transition: border-color 0.3s;
    background: transparent;
  }
  .login-box input[type="email"]:focus,
  .login-box input[type="password"]:focus {
    border-color: #d40000;
  }
  .login-box button {
    width: 100%;
    background-color: #d40000;
    border: none;
    color: white;
    padding: 12px 0;
    font-weight: bold;
    font-size: 16px;
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 2px 10px rgba(212, 0, 0, 0.3);
  }
  .login-box button:hover {
    background-color: #a10000;
    transform: translateY(-2px);
  }
  .login-box small {
    display: block;
    margin-top: 15px;
    font-size: 12px;
    color: #666;
    text-align: center;
  }
</style>

<div class="container">
  <div class="left">
    <form method="post" class="login-box">
      <div class="logo">
        <img src="{{ asset('/images/logo.png') }}" alt="Logo">
      </div>
      <input type="email" name="email" placeholder="Your email" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">SIGN IN</button>
      <small>Don't have account? Contact your manager for access.</small>
    </form>
  </div>
  <div class="right">
    <img src="{{ asset('/images/logo.png') }}" alt="Company Logo">
  </div>
</div>